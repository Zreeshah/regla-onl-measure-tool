
import React from 'react';
// The component is kept but no longer needs language switching functionality
const LanguageSwitcher: React.FC = () => {
  return null; // Return null since we don't need the language switcher anymore
};

export default LanguageSwitcher;
